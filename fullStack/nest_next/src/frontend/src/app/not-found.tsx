import * as React from 'react';

type NotFoundProps = {

}

export default function NotFound(props: NotFoundProps): JSX.Element {
  return (
    <html>
      <body>
        NotFound
      </body>
    </html>
  );
}