export default interface ITranslations {
  components: {
    nav: {
      PrimaryNav: {
        leftPanelTitle: string;
        rightPanelTitle: string;
        CoreLinks: {
          home: string;
          about: string;
          contact: string;
        };
        SessionLinks: {
          login: string;
          register: string;
        };
      };
    };
  };
}
